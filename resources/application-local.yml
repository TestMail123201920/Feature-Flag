spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/featureflag
    username: featureflag
    password: featureflag_local

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    com.company.featureflag: DEBUG
    org.hibernate.SQL: DEBUG
